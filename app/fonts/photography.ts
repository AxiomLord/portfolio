// Caveat is not bundled in the project. Keep this route font-independent so
// production builds never need to contact Google Fonts; the CSS role below
// falls back to the handwriting fonts available on the visitor's system.
export const photographyAccentFont = { variable: "" };
